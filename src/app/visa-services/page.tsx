'use client';
import styled from 'styled-components';

const Main = styled.main`
  min-height: calc(100vh - 80px);
`;

const VServicesPage = () => {
  return <Main>VServicesPage</Main>;
};

export default VServicesPage;

